# Central_service_provider
A website to route the queries to OpenChord Server

Usage : 
Clone this, Clone OpenchordServer on same PC, Bootsrap the OpenChordServer with nodes from OpenChord Repository;
